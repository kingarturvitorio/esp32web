# Django
DEBUG=False
SECRET_KEY=coloque-uma-chave-bem-grande-e-unica
ALLOWED_HOSTS=seu-dominio.com,EC2_PUBLIC_IP
CSRF_TRUSTED_ORIGINS=https://seu-dominio.com
DJANGO_SETTINGS_MODULE=core.settings

# Banco (rede interna do compose)
POSTGRES_DB=appdb
POSTGRES_USER=appuser
POSTGRES_PASSWORD=troque-isto
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Redis (interno)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# MQTT para o consumer (interno)
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_USER=esp32user
MQTT_PASSWORD=forte-se-abrir-internet

# Influx
INFLUX_URL=http://influxdb:8086
INFLUX_ORG=myorg
INFLUX_BUCKET=mybucket
INFLUX_TOKEN=supersecrettoken
INFLUX_ADMIN_PASSWORD=troque-isto

# (se for usar HTTPS no Nginx, configure domínio/caminhos de certs)

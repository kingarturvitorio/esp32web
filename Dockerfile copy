FROM python:3.12-slim
RUN apt-get update && apt-get install -y build-essential libpq-dev gdal-bin libgdal-dev libproj-dev netcat-traditional \
  && rm -rf /var/lib/apt/lists/*
WORKDIR /core
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .